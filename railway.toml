[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements_simple.txt"
installCommand = "pip install -r requirements_simple.txt"

[deploy]
numReplicas = 1
startCommand = "uvicorn api_simple:app --host 0.0.0.0 --port $PORT"